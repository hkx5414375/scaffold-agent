# {{.ProjectName}}

Generated by Scaffold Agent as a Go modular-monolith service with {{.Database.DisplayName}},
browser sessions, API tokens, and security audit events.
{{if .Business}}
It also contains a generated `{{.Business.EntityName}}` CRUD module with keyset
pagination, optimistic locking, transactional audit events, and explicit RBAC.
{{end}}
{{if .Tenancy}}
It also contains the `organization-tenancy` capability. Protected business data
is scoped by organization membership and the `X-Organization-ID` request header.
{{if .TenancyLifecycle}}
Organization version `0.3.0` adds an explicit owner, atomic ownership transfer,
and reversible deactivation. Inactive organizations retain data but cannot
authorize tenant business requests.
{{end}}
{{end}}
{{if .Jobs}}
It also contains the `background-jobs` capability with idempotent enqueue,
database leases, heartbeat renewal, bounded retry, and dead-letter state.
{{end}}
{{if .Files}}
It also contains the `file-assets` capability with tenant-scoped metadata,
streaming uploads, SHA-256 integrity metadata, and atomic local object writes.
{{end}}
{{if .Cache}}
It also contains the `application-cache` capability with cross-instance database
storage, tenant-aware keys, bounded JSON values, and explicit TTL cleanup.
{{end}}
{{if .JobAdmin}}
It also contains `job-administration` with payload-free status inspection and
audited dead-job retry. Only administrators receive its permissions.
{{end}}
{{if .Observability}}
It also contains `observability` with request correlation, safe structured access
logs, low-cardinality Prometheus metrics, and bounded database readiness.
{{end}}

## Requirements

- Go 1.27 or later
{{if eq .Database.Engine "postgresql"}}- PostgreSQL 14 or later
{{else}}- MySQL 8.4 or later
{{end}}

{{if .Admin}}- Node.js 22.12 or later
{{end}}

## Run locally

Set these environment variables before starting the service:

```bash
{{if eq .Database.Engine "postgresql"}}export DATABASE_URL='postgres://postgres:postgres@localhost:5432/{{.ProjectName}}?sslmode=disable'
{{else}}export DATABASE_URL='root:replace-me@tcp(127.0.0.1:3306)/{{.ProjectName}}'
{{end}}
export BOOTSTRAP_ADMIN_EMAIL='admin@example.com'
export BOOTSTRAP_ADMIN_PASSWORD='replace-with-at-least-12-characters'
export COOKIE_SECURE='false'
{{if .Files}}export FILE_STORAGE_ROOT='var/files'
{{end}}
go run ./cmd/server
```

`COOKIE_SECURE` defaults to `true`. Set it to `false` only for local HTTP.
The bootstrap account is created once; omit both bootstrap variables after that.
Embedded migrations run automatically under a {{.Database.DisplayName}} database lock.
The machine-readable HTTP contract is `api/openapi.yaml`.
{{if .Cache}}

Feature code uses `cache.Service` for derived or expensive-to-compute values. Keys
must include a feature namespace, and cached values must not contain secrets.
Missing, expired, and cross-tenant entries return `cache.ErrMiss`. Schedule bounded
calls to `cache.Service.PurgeExpired`; never issue an unbounded cache-table delete.
{{end}}
{{if .Jobs}}

Run the independent background worker with the same database:

```bash
export DATABASE_URL='replace-me'
export WORKER_ID='worker-1'
{{if .Notifications}}export SMTP_ADDRESS='smtp.example.com:587'
export SMTP_FROM='Example <no-reply@example.com>'
export SMTP_TLS_MODE='starttls'
export SMTP_USERNAME='replace-me'
export SMTP_PASSWORD='replace-me'
{{end}}go run ./cmd/worker
```

Feature code enqueues work through `internal/jobs.Service`. Use a stable
idempotency key for external requests. Add custom handlers in an unowned file such
as `internal/jobhandlers/email.go` and call `jobs.MustRegister` from `init`; the
managed worker imports that package and reads the registry snapshot at startup.
Handlers must honor context cancellation and must never log job payloads.
{{if .Notifications}}

Feature code calls `notifications.Service.EnqueueEmail` with a required stable
idempotency key after its own authorization. The worker validates SMTP settings
before polling and accepts only implicit TLS or STARTTLS. Keep credentials in the
runtime secret store; queued message bodies are sensitive database content.
{{end}}
{{end}}

## HTTP API

- `GET /healthz`
{{if .Observability}}- `GET /readyz`
- `GET /metrics`
{{end}}
- `POST /api/v1/auth/login`
- `POST /api/v1/auth/logout`
- `GET /api/v1/auth/me`
- `POST /api/v1/auth/tokens`
{{if .Tenancy}}
- `POST /api/v1/organizations`
- `GET /api/v1/organizations`
{{if .TenancyMembers}}
- `GET /api/v1/organizations/{organizationID}/members`
- `POST /api/v1/organizations/{organizationID}/invitations`
- `PATCH /api/v1/organizations/{organizationID}/members/{userID}`
- `DELETE /api/v1/organizations/{organizationID}/members/{userID}`
- `POST /api/v1/organization-invitations/accept`
{{if .TenancyLifecycle}}- `PATCH /api/v1/organizations/{organizationID}`
- `POST /api/v1/organizations/{organizationID}/ownership-transfers`
- `POST /api/v1/organizations/{organizationID}/deactivation`
- `POST /api/v1/organizations/{organizationID}/reactivation`
{{end}}
{{end}}
{{end}}
{{if .Files}}
- `POST /api/v1/files`
- `GET /api/v1/files`
- `GET /api/v1/files/{id}`
- `GET /api/v1/files/{id}/content`
- `DELETE /api/v1/files/{id}`
{{end}}
{{if .JobAdmin}}
- `GET /api/v1/jobs`
- `POST /api/v1/jobs/{id}/retry`
{{end}}
{{if .Business}}
- `POST {{.Business.RoutePath}}`
- `GET {{.Business.RoutePath}}`
- `GET {{.Business.RoutePath}}/{id}`
- `PUT {{.Business.RoutePath}}/{id}`
- `DELETE {{.Business.RoutePath}}/{id}?version={current_version}`
{{end}}

Browser authentication uses an HttpOnly, SameSite=Lax cookie. Automation uses
`Authorization: Bearer <token>`. Only token digests and password hashes are stored.
{{if .Observability}}
Every response includes a validated or generated `X-Request-ID`. Access logs omit
query strings and bodies. `/readyz` waits at most two seconds for the database and
returns only a generic status; `/metrics` exposes aggregate process counters with
no user-derived labels. Protect these operational endpoints at the deployment
boundary when they are not intended for public network access.
{{end}}
{{if .Tenancy}}
Create an organization after signing in, then send its identifier in
`X-Organization-ID` for every protected business request. The generated stores
include the organization in reads, writes, uniqueness, pagination, and optimistic
locking predicates; a valid record identifier from another organization is still
reported as not found.
{{if .TenancyMembers}}
Administrators can create 72-hour email-bound invitations, change member roles,
and remove members. Invitation secrets are stored only as SHA-256 digests and are
returned once for delivery. Role changes and removals are serialized per
organization so the final administrator cannot be removed or demoted.
{{end}}
{{end}}
{{if .Admin}}
## Administration UI

```bash
cd web/admin
npm ci
npm run dev
```

The Vite development server proxies `/api` and `/healthz` to `localhost:8080`.
Run `npm run lint`, `npm test`, `npm run build`, and `npm run format:check`
before committing changes.
{{end}}

## Verify

```bash
go test ./...
go vet ./...
```

To include the isolated live {{.Database.DisplayName}} integration test, provide a dedicated
test server. The test creates and removes only its own random {{if eq .Database.Engine "postgresql"}}schema{{else}}database{{end}}:

```bash
{{if eq .Database.Engine "postgresql"}}TEST_DATABASE_URL='postgres://postgres:postgres@localhost:5432/{{.ProjectName}}?sslmode=disable' go test ./...
{{else}}TEST_DATABASE_URL='root:replace-me@tcp(127.0.0.1:3306)/' go test ./...
{{end}}
```
