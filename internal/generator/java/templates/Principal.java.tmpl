package {{.PackageName}}.identity;

public record Principal(
        String userId,
        String email,
        String role,
        String method{{if .Tenancy}},
        String organizationId{{end}}) {}
