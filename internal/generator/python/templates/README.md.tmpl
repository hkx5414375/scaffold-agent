# {{.ProjectName}}

由 Scaffold Agent 确定性生成的 FastAPI 服务，支持 {{.Database.DisplayName}}、浏览器会话、API Token、RBAC、审计日志和 Alembic 数据库迁移。{{if .Business}}项目还包含由 Blueprint 定义的 `{{.Business.ModuleName}}/{{.Business.EntityName}}` 完整 CRUD。{{end}}{{if .Tenancy}}项目启用了组织多租户，业务数据和权限按组织隔离。{{end}}{{if .Jobs}}项目启用了可靠后台任务和独立 Worker。{{end}}{{if .Files}}项目启用了有界文件上传、下载和安全本地对象存储。{{end}}{{if .Cache}}项目启用了跨实例一致的数据库 JSON 缓存。{{end}}{{if .JobAdmin}}项目启用了不暴露载荷的任务元数据查询和死信重试。{{end}}{{if .Observability}}项目启用了安全请求关联、访问日志、低基数指标和 2 秒数据库就绪探测。{{end}}{{if .CSVTransfer}}项目启用了 Blueprint 字段顺序驱动的原子 CSV 导入与审计导出。{{end}}{{if .Approvals}}项目启用了职责分离、不可变事件和乐观并发的单阶段审批。{{end}}{{if .Catalog}}项目启用了带审计、发布状态和公开读取边界的商品目录。{{end}}

## 环境要求

- Python 3.12 或 3.13
- uv 0.12.8
- 可用的 {{.Database.DisplayName}} 数据库

## 本地启动

```powershell
$env:DATABASE_URL = "{{.Database.ExampleURL}}"
$env:BOOTSTRAP_ADMIN_EMAIL = "admin@example.com"
$env:BOOTSTRAP_ADMIN_PASSWORD = "replace-with-at-least-12-characters"
$env:COOKIE_SECURE = "false"
{{- if .Files}}
$env:FILE_STORAGE_ROOT = "C:\\replace-with-runtime-storage"
{{- end}}
uv sync --frozen --all-groups
uv run uvicorn {{.PackageName}}.main:app --reload --port 8080{{if .Observability}} --no-access-log{{end}}
```
{{if .Jobs}}
另开一个终端启动独立任务 Worker：

```powershell
$env:DATABASE_URL = "{{.Database.ExampleURL}}"
$env:WORKER_ID = "worker-local-1"
uv run python -m {{.PackageName}}.jobs.worker
```

业务代码通过 `JobService.enqueue` 提交最大 1 MiB 的 JSON 载荷和稳定幂等键。Worker 会处理抢占、续租、指数退避、死信和过期租约回收；Handler 必须响应传入的取消事件，不得记录载荷。
{{end}}
{{if .Notifications}}
启动 Worker 前必须提供 TLS-only SMTP 运行时配置：

```powershell
$env:SMTP_ADDRESS = "smtp.example.com:587"
$env:SMTP_FROM = "app@example.com"
$env:SMTP_TLS_MODE = "starttls" # 或 tls
$env:SMTP_USERNAME = "mailer"  # 可选，但必须与密码同时提供
$env:SMTP_PASSWORD = "replace-me"
```

业务能力在完成自身授权后调用 `NotificationService.enqueue_email`，必须提供稳定幂等键。单封邮件的文本与 HTML 正文合计最多 512 KiB；项目不生成“任意发邮件”HTTP 接口。正文会保存在可靠任务载荷中，部署方必须为数据库、备份和保留策略提供相应的数据保护。
{{end}}
{{if .Files}}
文件接口以流式方式接收最大 10 MiB 的单个 multipart 文件，保存 SHA-256 和租户范围元数据，不向 API 暴露内部存储键。`FILE_STORAGE_ROOT` 必须指向独立运行时目录；目录不能位于源码仓库内，也不能包含符号链接。元数据写入或文件删除失败时会执行跨资源补偿。
{{end}}
{{if .Cache}}
`CacheService` 在 {{.Database.DisplayName}} 中保存跨进程一致的 JSON，租户项目按组织隔离，非租户项目固定使用 `global` 范围。键最多 191 字节，值最多 256 KiB，TTL 必须大于零且不超过 30 天；不存在、过期和跨租户读取统一返回 `CacheErrorKind.MISS`。`purge_expired` 每次最多清理 10,000 行。

业务代码应使用稳定命名空间并在权威数据变更后主动失效，不得缓存凭据或生成通用缓存 HTTP 接口。需要 Redis 时只替换仓储适配器，并保留范围、大小、过期、Miss 和有界清理契约。
{{end}}
{{if .JobAdmin}}
管理员使用 `jobs:read` 查看最多 100 条任务元数据，使用 `jobs:manage` 把当前组织内的死信任务重新放回队列。API、OpenAPI 和管理端都不暴露任务载荷、作用域键或幂等键；重试会在一个数据库事务中清理租约和错误状态并写入 `jobs:retry` 审计事件。不得通过管理接口查看或修改载荷、接管运行中任务或绕过组织范围。
{{end}}
{{if .Observability}}
每个 HTTP 响应都包含经过校验或新生成的 `X-Request-ID`。JSON 访问日志只包含请求 ID、方法、不带查询串的路径、状态和耗时；不会读取请求体，也不会记录 Header 或异常值。`GET /metrics` 只返回无标签的进程内聚合指标；`GET /readyz` 把连接获取和校验整体限制为 2 秒，只返回 `ready` 或 `unavailable`。公开部署必须在入口或私网边界保护这两个无需应用认证的运维接口。
{{end}}
{{if .CSVTransfer}}
管理员使用 `{{.Business.PermissionPrefix}}:import` 从 UTF-8 CSV 创建记录，使用 `{{.Business.PermissionPrefix}}:export` 下载当前{{if .Tenancy}}组织{{else}}应用{{end}}范围内按 ID 排序的记录。表头固定为 Blueprint 可变字段顺序；布尔值只接受 `true`/`false`，整数使用十进制 int64，时间使用 RFC3339，空白可选单元格变为 null。单次最多 5 MiB 和 1000 行；导入先校验完整文档，再把所有业务行和审计事件放入一个事务，任意冲突都不产生部分数据。导出字符串使用可逆的前置单引号防止电子表格公式执行。
{{end}}
{{if .Approvals}}
`approval-workflows` 只实现 Blueprint 明确声明的 `pending → approved/rejected/cancelled` 单阶段流程。`approvals:submit` 与 `approvals:mine` 供发起人提交、查看和撤回自己的请求；`approvals:read` 与 `approvals:decide` 供审核人查看和决定。发起人不能审核自己的请求，驳回必须填写理由，撤回只允许原发起人，每个终态请求都必须携带当前版本。数据库会重复校验组织、待处理状态、版本和参与者规则，并把状态、不可变事件与审计放在同一事务。审批只记录决定，不会隐式修改 `{{.Business.EntityType}}`。
{{end}}
{{if .Catalog}}
`commerce-catalog` 独立于 Blueprint 通用 CRUD。后台通过 `/api/v1/catalog/products` 创建、修改、发布和归档商品；SKU 与币种统一为大写，金额使用最小货币单位，金额和版本在 JSON 中使用十进制字符串。商品只允许 `draft → active → archived`，归档后不可修改或重新发布，每次成功写入都与审计事件在同一事务提交。匿名 `/api/v1/storefront/products` 只返回已上架商品{{if .Tenancy}}和状态为 active 的组织；公开请求必须携带 `X-Organization-ID`，Nuxt 服务端从 `SCAFFOLD_ORGANIZATION_ID` 注入该值{{end}}。
{{end}}
{{if .Admin}}
另开一个终端启动共享管理端：

```powershell
cd web/admin
npm ci
npm run dev
```
{{end}}

启动过程会在接受 HTTP 流量前执行 Alembic 迁移。管理员引导配置必须同时提供邮箱和密码；已有同邮箱管理员不会被覆盖密码或降级权限。

## 质量门禁

```powershell
uv lock --check
uv run ruff format --check .
uv run ruff check .
uv run mypy src tests
uv run bandit -c pyproject.toml -r src
uv run pytest
```

## 认证约定

- `POST /api/v1/auth/login` 创建 24 小时、`HttpOnly`、`SameSite=Lax` 会话 Cookie。
- `Authorization: Bearer <token>` 使用只在创建时返回一次的 90 天 API Token。
- 数据库只保存凭证的 SHA-256 摘要，密码使用有参数上限的 PBKDF2-SHA256。
- `POST /api/v1/auth/tokens` 要求 `identity:token:create` 权限。
- 对外 API 契约固定在 `api/openapi.yaml`，运行时文档不能代替版本化契约。
{{if .Tenancy}}

## 组织隔离约定

- `POST /api/v1/organizations` 创建组织，并在同一事务中把创建者加入为管理员。
- `GET /api/v1/organizations` 只返回当前身份实际加入的组织。
- 每个业务请求都必须发送 `X-Organization-ID`；服务端重新验证成员关系和组织内角色，不信任客户端角色。
- 业务查询、变更、游标分页和唯一约束都包含组织标识；跨组织读取统一返回不存在。
{{- if .TenancyMembers}}
- 管理员可以创建绑定邮箱且 72 小时有效的邀请、变更角色和移除成员；邀请明文只返回一次，数据库只保存 SHA-256 摘要。
{{- end}}
{{- if .TenancyLifecycle}}
- 创建者同时成为所有者。管理员可改名，只有所有者可转移所有权、停用或恢复组织。
- 停用不删除数据，但会阻断新的租户授权；恢复接口只依赖身份认证并在事务内重新核对所有权。
{{- end}}
{{end}}

## 目录边界

- `src/{{.PackageName}}/identity`：领域记录、用例、持久化和 HTTP 适配器。
- `src/{{.PackageName}}/migration`：不可跳号的数据库迁移。
{{if .Tenancy}}
- `src/{{.PackageName}}/tenancy`：组织创建、成员范围授权和租户上下文。
{{end}}
{{if .Jobs}}
- `src/{{.PackageName}}/jobs`：可靠任务领域、事务仓储、服务和独立 Worker。
{{end}}
{{if .Notifications}}
- `src/{{.PackageName}}/notifications`：内部通知服务、严格任务解码和 TLS-only SMTP 适配器。
{{end}}
{{if .Files}}
- `src/{{.PackageName}}/files`：租户范围文件元数据、事务仓储、用例和原子本地对象存储。
{{end}}
{{if .Cache}}
- `src/{{.PackageName}}/cache`：跨实例 JSON 缓存用例、原子方言 Upsert 和有界过期清理。
{{end}}
{{if .JobAdmin}}
- `src/{{.PackageName}}/jobadmin`：不暴露载荷与幂等键的任务元数据查询，以及仅死信可用的审计重试。
{{end}}
{{if .Observability}}
- `src/{{.PackageName}}/observability`：请求关联、安全访问日志、无标签 Prometheus 指标和通用 500 恢复。
{{end}}
{{if .CSVTransfer}}
- `src/{{.PackageName}}/{{.Business.PackageName}}/transfer`：Blueprint CSV 解码、原子导入、租户范围审计导出和 HTTP 适配器。
{{end}}
{{if .Approvals}}
- `src/{{.PackageName}}/approvals`：单阶段审批领域、事务仓储、不可变事件、用例和 HTTP 适配器。
{{end}}
{{if .Catalog}}
- `src/{{.PackageName}}/catalog`：商品目录领域、事务仓储、发布状态机、管理与公开 HTTP 适配器。
{{end}}
- `tests`：单元、HTTP、架构和真实数据库契约测试。
- `api/openapi.yaml`：供人和 AI 工具读取的稳定接口真相源。
{{if .Business}}
- `src/{{.PackageName}}/{{.Business.PackageName}}`：`{{.Business.EntityName}}` 的领域、仓储和 HTTP 垂直切片。
{{end}}
