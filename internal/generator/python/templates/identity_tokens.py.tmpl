"""Opaque credential generation and digest-only persistence."""

import hashlib
import secrets


def new_token() -> str:
    """Return a high-entropy opaque credential."""
    return secrets.token_urlsafe(32)


def digest_token(token: str) -> str:
    """Return the SHA-256 digest stored by persistence."""
    return hashlib.sha256(token.encode()).hexdigest()
